<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anshun.dms.mapper.AiAgentFeedbackMapper">
  <select id="selectActiveUserId" resultType="int">
    SELECT user_id FROM t_user WHERE username=#{username} AND status='active'
  </select>

  <insert id="upsertForOwnedRun">
    INSERT INTO ai_agent_feedback (run_id, user_id, rating, comment)
    SELECT #{runId}, #{userId}, #{rating}, #{comment}
    FROM ai_agent_run
    WHERE run_id=#{runId} AND user_id=#{userId}
    ON DUPLICATE KEY UPDATE rating=VALUES(rating), comment=VALUES(comment), update_time=CURRENT_TIMESTAMP
  </insert>
</mapper>
